digraph graphname {
    node [style=rounded, shape=Mrecord];
    graph [rankdir=LR];
    output[kind=output];
    a -> b -> c;
    d -> e -> f;
    a -> g;
    g [kind=add];
    g:hi -> output:diffuse;
}
